// TODO(step05): app Hono real (REST + SSE) implementado na etapa 05.
export function placeholder(): void {}

if (import.meta.url === `file://${process.argv[1]}`) {
  process.stderr.write('aideck server: not implemented yet (etapa 05)\n')
  process.exit(0)
}
